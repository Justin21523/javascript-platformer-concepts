# 遊戲玩法手冊 / Controls & Modes

## 基本操作
- 移動：WASD / 方向鍵
- 跳躍：Space
- 暫停：`（反引號），單步：.
- Debug：F1 Overlay、F2 碰撞框、F3 慢動作、F4 網格、F6 面板、F7 關卡選單
- 截圖：F12（觸發 debug-screenshot 事件）

## Hyperdrive 超速模式
- 能量自動回充，滿格時按 `K` 啟動；`H` 切換風格 (Inferno/Shadow Skull/Void Flame)。
- 啟動時附帶震動、尾焰與殘影，尾焰會對敵造成傷害；光道提示與 HUD 會同步顯示剩餘時間。

## 光道與過場選單
- 每關出生點右方會生成光道，靠近時 HUD 顯示距離與箭頭。
- 撞到光道進入 1 秒過場，出現選單：`1` 慶祝、`2` 下一關、`3` 重玩本關。

## 慶祝模式（派對場）
- 平坦無限地面，無敵遊玩。
- 操作：`V` 桌布背景（按住）、`C` 分身淡出、`F` 彩帶噴發、`Y` 派對音樂切換、`N` 離開返回關卡。
- 自動效果：煙火、彩帶、氣球循環播放。

## 無敵桌布模式
- 按住 `X` 清空場景，只留玩家與桌布特效；放開恢復原畫面。
- `Z` 切換桌布主題（含幾何三角彈幕）、`M` 調整粒子量（並自動依 FPS 調整），`J/L/U` 放煙火/螺旋彈幕。

## 其他提示
- HUD 會顯示 Hyperdrive 能量、光道距離、桌布/慶祝狀態、音樂狀態等。
- 派對音樂使用內建 WebAudio，可用 `Y` 觸發/關閉。
- 碎片：擊敗敵人掉落，顯示於 HUD，慶祝時可消耗 3 碎片抽取神器。
- 神器稀有度：常見/稀有/史詩，可能提供攻速/移速、尾焰灼燒、星爆增幅、持久 Hyper、雙跳、落地震擊、擊殺回血、碎片加成、元素冰/雷等。

## 戰鬥與技能
- 近戰/射擊：`J` 輕斬、`L` 重斬、`U` 射擊、`I` 上斬、`O` 旋斬、`P` 波紋、`G` 星爆（長按持續）。
- 防禦：`B` 盾牌格擋（面向方向有效，帶冷卻）。
- Hyperdrive：`K` 啟動（能量滿）、`H` 切換風格；尾焰與前撞會傷害敵人。

## HUD / Debug / UI
- Overlay (F1) 顯示：FPS、各系統耗時、玩家座標/速度、HP、攻擊 CD、Hyperdrive/光道/桌布/慶祝狀態。
- HUD 左上：能力能量條與風格、光道距離箭頭提示、慶祝/桌布/音樂狀態。
- 光道過場選單：1=慶祝 2=下一關 3=重玩；過場中玩家靜止，戰鬥/刷怪暫停。
- F2 碰撞框、F3 慢動作、F4 網格、F6 Debug 面板、F7 關卡選單（選關/重生）。
- 事件點：地圖隨機出現祭壇（靠近顯示提示，按 T 觸發：碎片/回血/免費神器）。

## 關卡與流程
- 關卡來源：預設靜態關卡與程序生成關卡（寬 64、高 24，主題 plains/ruins/clouds）。通關後自動產生新的程序關卡（不同種子/主題）繼續遊玩。
  - 程序關卡可包含多個區段（3–5 段，寬度加倍），支持 tileset 動態混合；區段交界會生成休息區（不刷怪，可遇到事件祭壇）。
- 流程：出生 → 向右找到光道 → 過場選單（慶祝/下一關/重玩）。
- 碰撞：InfiniteWorld 水平循環，地面以下自動填實心防掉落，天空層可重複循環。

## NPC 與行為
- Ground Patrol：地面巡邏，近距離接戰。
- Flying Charger：空中快速衝刺接近玩家，攻擊時會高速衝刺並側向釋放彈幕。
- Vertical Sentinel：垂直巡航，攻擊時蓄力後發射貫穿光束。
- 刷怪：SpawnSystem 依玩家附近密度動態生成地面/空中敵人（慶祝或桌布模式下暫停刷怪）。

## 視覺與音效回饋
- 玩家受擊：畫面震動與紅色閃屏。
- Hyperdrive：啟動有震動，尾焰與前撞傷害敵人。
- 慶祝/派對：煙火、彩帶、氣球循環，`Y` 切換派對音樂。

## 載入提示
- 進入遊戲時顯示置中載入條，讀取資產進度百分比，完成後自動隱藏。

## 後續待實作（規劃）
- 超能力/神器三選一：通關或收集碎片後跳出強化選擇（攻速、移速、尾焰灼燒、盾格檔反擊等）。
- 多區段程序地圖：每關含多個主題區段（plains/ruins/clouds/tileset mix），含休息區與次要挑戰點。
- 事件/試煉：隨機事件房或條件挑戰（無傷、限時）獎勵高級詞綴。
- 新敵人：盾兵（背後破防）、投擲兵（拋物線炸彈/毒雲）、支援型（治療增益）、頭目分階段行為。
- 移動/戰鬥擴展：二段跳/衝刺/壁跳、元素射擊切換（火/冰/雷），近戰連段計數與強化。
- 詳細規劃：
  1) 超能力樹與神器詞綴：碎片貨幣；光道/慶祝後升級 Hyperdrive 時間、尾焰灼燒、星爆彈幕、盾牌完美格擋反擊；隨機掉落被動詞綴（三選一：攻速/移速/雙跳/落地震擊/擊殺回血）。  
  2) 更廣闊地圖：分區世界 3–5 區段串接（主題可切換 plains→ruins→clouds），插入休息區；多層高度網絡/塔區/地洞，光道可能在上層或下層，配合指示箭頭；次要目標（收集點/挑戰房）給暫時 buff。  
  3) 敵人深度：盾兵需背後攻擊；投擲兵拋炸彈/毒雲；支援型治療/增益；Boss/小頭目分階段（衝刺+彈幕+召喚）。  
  4) 玩家擴展：二段跳/衝刺/壁跳，桌布模式可漂浮；近戰連段計數提升攻速或傷害；投射物元素火/冰/雷（灼燒/緩速/鏈擊）。  
  5) 事件與風險：事件房（增益/陷阱/交易）；試煉（無傷/限時）給神器或能力值；危險區高密度刷怪但高獎勵。  
  6) 視覺/音效：光道與 HUD 色彩隨 Hyperdrive/桌布/慶祝切換；元素/受擊/Boss 警報音；天氣粒子（雨/霧/雪）影響移動或視野。  
  7) Meta 進程：簡易存檔（最高進度、解鎖超能力/神器）；通關後提供下場次加成（初始能量/起始神器）。  
